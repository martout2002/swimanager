# --- Local development (docker compose up -d) ---
DATABASE_URL="postgresql://postgres:swim@localhost:5433/swimanager"

# --- Supabase (production) ---
# Project Settings -> Database -> Connection string -> Transaction pooler (port 6543),
# with ?sslmode=require appended. Use the pooler host, not db.<ref>.supabase.co.
# DATABASE_URL="postgresql://postgres.<ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres?sslmode=require"


# openssl rand -base64 48
AUTH_SECRET=""
